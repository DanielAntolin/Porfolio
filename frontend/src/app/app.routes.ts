import { Routes } from '@angular/router';
import { TerminalLayoutComponent } from './features/terminal-layout/terminal-layout.component';

export const routes: Routes = [{ path: '', component: TerminalLayoutComponent }];
